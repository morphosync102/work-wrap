[project]
name = "work-wrap"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "PyYAML>=6.0",
    "holidays>=0.46",
    "openpyxl>=3.1",
    "selenium>=4.0",
    "webdriver-manager>=4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
